# railway.toml
# Railway deployment configuration for CineMind backend

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn backend.api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
